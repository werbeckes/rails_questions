<h1>This is your home page</h1>

<%= button_to "Submit answers", answer_user_path(current_user), method: 'get' %>

<br>
<div id="question list">
  <%= render @questions %>
</div>
<br>

<%= button_to "Add a new question", new_user_question_path(current_user), method: 'get' %>

<%= link_to "send reminder email now", send_reminder_path(current_user) %>
